# Model Delivery Pipeline POC - runtime dependencies
# The pipeline itself is fully local: no cloud services and no network access
# at runtime. Four packages, and one of them is only used when the dashboard
# is put behind a real web server instead of Flask's development one.

numpy==2.5.2                 # geometry math
fast-simplification==0.2.0   # quadric edge-collapse decimation (LOD generation)
Flask==3.1.3                 # operations dashboard + viewer host

# Production WSGI server. Not installed on Windows (gunicorn is POSIX-only);
# local runs use `python -m modelops serve` instead.
gunicorn==23.0.0 ; sys_platform != "win32"
